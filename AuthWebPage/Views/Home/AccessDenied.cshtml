@{
    ViewBag.Title = "Acceso denegado";
    Layout = "_Layout";
}

<div class="d-flex flex-column align-items-center justify-content-center min-vh-100">

    <div class="p-5 rounded-4 shadow-lg text-center"
         style="background: rgba(255, 255, 255, 0.75);
                backdrop-filter: blur(8px);
                border: 1px solid rgba(255,255,255,0.4);
                max-width: 550px; width: 90%;">

        <h1 class="fw-bold mb-3" style="color:#7e22ce;">🚫 Acceso denegado</h1>

        <p class="lead mb-4" style="color:#4c1d95;">
            Debes iniciar sesión para acceder a esta página exclusiva 💎
        </p>

        <div class="d-flex justify-content-center gap-3 mt-3">
            <a asp-controller="Session" asp-action="Login"
               class="btn fw-bold px-4 py-2"
               style="background-color:#a855f7;
                      color:white;
                      border-radius:12px;
                      box-shadow:0 0 10px rgba(168,85,247,0.4);
                      transition: all 0.3s ease;">
                Iniciar sesión
            </a>

            <a asp-controller="Session" asp-action="Register"
               class="btn fw-bold px-4 py-2"
               style="background-color:white;
                      color:#6d28d9;
                      border:2px solid #c4b5fd;
                      border-radius:12px;
                      transition: all 0.3s ease;">
                Registrarme
            </a>
        </div>

        <hr class="my-4" style="border-color:#e9d5ff;">

        <p class="mb-0" style="color:#4c1d95;">
            🌸 Solo usuarios autenticados pueden entrar aquí.
        </p>
    </div>
</div>